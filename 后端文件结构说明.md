# DeepAudit 后端文件结构说明

这里是 `backend/src/main/java/com/deepaudit` 下后端的整体文件结构与核心代码说明，重点梳理了 **规则（Rule）**、**算子（Operator）** 以及 **大模型（LLM）调用** 相关的类。

## 目录结构概览

```text
com.deepaudit
├── DeepauditApplication.java      # Spring Boot 启动类
├── api/                           # 控制器层 (Controllers)、数据传输对象 (DTOs) 和 全局异常处理
├── config/                        # 配置类（特别是大模型 LLM 配置）
├── engine/                        # 核心质控规则引擎（DSL 解析与执行、算子库）
├── persistence/                   # 数据库持久化层（Entity 实体与 Repository 接口）
└── service/                       # 核心业务逻辑与大模型 AI 服务层
```

---

## 1. `config` (配置层 - 大模型相关)

主要处理 DeepSeek / LLM API 的自动装配和配置读取。

- **`LlmProperties.java`**: 大模型属性配置类，映射 `.env` 或 `application.yml` 中的参数（如 `DEEPSEEK_API_KEY`、Base URL 等）。
- **`LlmAutoConfiguration.java`**: 结合配置信息，利用 LangChain4j 等工具初始化 AI Service 实例（大模型客户端），供后续 Service 层依赖注入使用。

---

## 2. `engine` (引擎层 - 规则与算子核心)

负责整个质控引擎的 DSL（领域特定语言）解析、算子的执行，属于不依赖外部 IO（除缓存外）的纯逻辑函数层。

- **`RuleEvaluator.java`**: **规则解释执行器**。它是整个质控的核心计算中枢，接收一条解析好的 DSL 规则树和单份病历记录，递归判定病历是否违规（如执行 `eq`, `notNull`, `dateBefore`, `icdCodeExists` 等操作）。
- **`CustomOperatorRegistry.java`**: **自定义算子注册表**。负责在内存中管理所有业务方新增的扩展算子库（Operator Templates），供引擎解析 `custom` 类型的操作符。
- **`RuleDslValidator.java`**: **DSL 校验器**。防线类代码，负责校验从 LLM 生成或前端保存传来的 JSON 规则结构是否合法（防止语法错误或注入），在入库落盘前强制把关。
- **`FieldAccessor.java`**: **字段访问器**。辅助引擎根据 DSL 中的字段名（如 `patientName`），动态提取当前病历中对应的值。
- **`IcdDictCache.java`**: **ICD 字典缓存**。用于快速判定 ICD 诊断编码或名称的合法性和一致性（用于 `icdCodeExists` 和 `icdNameMatches` 算子）。

---

## 3. `service` (业务与 AI 服务层 - 大模型调用)

此包整合了大量具体的业务逻辑和对接大模型的自然语言处理任务。

### A. 大模型生成（LLM Calling）相关：

- **`RuleGeneratorService.java`**: **自然语言转规则引擎**。核心类。负责将用户输入的自然语言（如：“年龄大于 65 岁且诊断为高血压时，必须有血压监测记录”）喂给大模型，生成系统可识别的 JSON DSL。附带容错处理，清洗大模型可能误加的 Markdown 格式，以及处理 LLM 同步生成的自定义算子。
- **`RuleDslGenerator.java`**: AI Service 的接口代理，可能内部封装了 LangChain4j 的 `generate` 契约。
- **`OperatorGeneratorService.java`**: **大模型算子生成服务**。传入算子参数名和自然语言描述，调用大模型生成带有占位符（`$ref`）的自定义算子 DSL（BodyDSL），让 AI 帮我们写规则判断逻辑。
- **`OperatorBodyDslGenerator.java`**: 配合上方 Service 使用的 AI 接口契约，专门输出算子的 JSON DSL。
- **`ViolationExplainerService.java`**: **违规解释服务**。当病历未通过规则时，调用大模型，将冰冷的违规代码解释为人类易读的质控建议。
- **`TestingAssistantService.java`**: 测试助手服务。使用大模型来 mock、分析或回答特定的质控相关问题。

### B. 核心业务处理：

- **`CheckService.java`**: **病历质控检查服务**。串联业务：拿到病历，遍历规则库，调用 `RuleEvaluator` 执行检查，记录通过/违规状态。
- **`RuleService.java`**: 规则的基础 CRUD 管理。
- **`MedicalRecordExtractor.java` / `MedicalRecordImportService.java`**: 病历抽取和导入的服务层，前者可能也涉及通过大模型进行病历非结构化文本的字段级提取。
- **`MedicalRecordMockFiller.java`**: 随机或智能生成假病历数据用于质控测试。

---

## 总结：数据流向

1. **写规则：** 用户输入大白话 -> `RuleGeneratorService` (LLM) 转换为 JSON DSL -> `RuleDslValidator` 校验 -> 入库。
2. **写算子：** 用户指定参数与逻辑 -> `OperatorGeneratorService` (LLM) 转换为算子模板 -> 加载进 `CustomOperatorRegistry` 缓存。
3. **查病历：** `CheckService` 接收病历 -> `RuleEvaluator` 运行引擎评估各个规则（基础规则 + 查注册表中的自定义算子）-> 返回并保存结果。
