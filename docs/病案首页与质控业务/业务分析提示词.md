# 业务分析提示词

> **用途**：记录本次 DeepAudit 病案首页质控 MVP 设计/开发协作中，由用户发出的全部提示词，按对话顺序保留原貌。
>
> **配套产出**：
> - 《病案首页质控MVP开发计划书.md》—— 11 章工程实施计划
> - 《病案首页质控MVP开发执行流程.md》—— 4 阶段施工 checklist
>
> **会话时间**：2026-05-03

---

## 一、需求与设计澄清阶段

### 提示词 #1 —— 启动需求

```
现在根据现有的doc文档，帮我写一份开发计划书，按照下面的要求：做一个对病案首页进行至少两项质控规则检查的页面（比如缺项检查、逻辑检查之类的）；

设计要求：

1、设计一个设置质控检查规则的界面，可以对规则增加、修改、删除【考虑已启用的规则怎么处理】；
2、设计一个导入电子档病案首页的页面展现。【业务背景：它院病案首页PDF导入本院系统中】；
3、设计一个功能可以根据事先设置好的检查规则对导入的病案首页进行规则检查并指出；
```

### 提示词 #2 —— 7 问澄清答复 + 追加 LLM 落点要求

```
1. b
2. 功能模块
3. vue + Java(langchain4j) + pgv
4. OCR自研发困难吗？
5. 先只实现两点简易的MVP
6. 采用国家卫健委改良版，后续会涉及到病案首页分的更新
7. 预计两天内做一个初版出来，暂时不需要对齐KickOff 里的 M1–M4 路线图
8. 我希望加入llm，你看看建议在什么地方使用大模型
```

### 提示词 #3 —— 选定 OCR 方案与章节策略

```
方案4，全要，10中要保留我与你对话的提示词和你的答复
```

### 提示词 #4 —— 第 3 章架构 5 问澄清答复

```
1. 可以
2. a
3. SSE/WebSocket（用于流式输出 LLM 解释）
4. 跳过认证
5. 先做单一当前版本
```

### 提示词 #5 —— 第 3 章确认推进

```
确认无误，继续
```

---



## 二、交付与运维阶段

### 提示词 #13 —— 推送远程仓库

```
帮我推送到远程仓库
```

### 提示词 #14 —— 选用 token 推送方式

```
2. 我用 .token 文件推（如果你确认这是 GitHub PAT）：我读取并组装临时 URL https://<token>@github.com/Llffan/DeepAudit.git 推一次，不持久化到 git config。
```

### 提示词 #15 —— 修复 Mermaid 流程图错误

```
3.2 逻辑架构 和 6.1 用户流程总览下面的 流程图报错了，排查一下是什么问题并修复
```

### 提示词 #16 —— 输出开发流程指引

```
现在你读取文档，告诉我开发的流程分步骤怎么进行
```

### 提示词 #17 —— 落地为执行流程文档并推送

```
生成计划文档md并推送仓库
```

### 提示词 #18 —— 归档本对话提示词

```
现在将前面我与你的对话记忆（只是我提问的部分）存储到md文档中命名为业务分析提示词
```

---

## 提示词使用价值简注

| 提示词阶段 | 价值 | 复用建议 |
|---|---|---|
| #1 | 一句话说清三件事的需求陈述模板 | 后续模块需求时可参照"做一个 X / 设计要求 1/2/3"格式 |
| #2 | 用极简编号回复批量回答澄清问题 | 与 AI 高效协作的对齐方式，省时间 |
| #3 | 关键技术路线决策（多模态 LLM 替代传统 OCR） | 类似项目可参考此判断逻辑 |
| #4 / #6 | 配置类决策（部署/认证/规则版本/字段范围/DSL 形式） | 是工程边界的锚点 |
| #5 / #7-#12 | "继续"作为推进信号 | 表明用户偏好快节奏，不逐节细审 |
| #15 | Mermaid 渲染问题的精确定位（指出具体章节） | 报错反馈的最佳实践：定位到节而非全局 |
| #16-#17 | 把设计文档转化为可执行清单的诉求 | 工程项目从"想清楚"到"做出来"的关键转折点 |

---

*— 提示词归档完 —*
