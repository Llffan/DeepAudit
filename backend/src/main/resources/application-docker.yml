spring:
  datasource:
    url: jdbc:postgresql://db:5432/deepaudit
    username: deepaudit
    password: deepaudit

deepaudit:
  llm:
    # 在这里填写你的 GEMINI API Key
    # 优先级：环境变量 GEMINI_API_KEY > 这里的配置
    api-key: ${GEMINI_API_KEY:AIzaSyBnV8AtG_mDU-f_oe3O0vQ7qlrtYS35Uhk}
