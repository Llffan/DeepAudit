# DeepAudit 项目架构说明

> 本文档讲清楚 **DeepAudit 病案首页质控系统**的每个目录、每个组件分别在做什么，
> 以及它们之间是如何协作的。新成员或离开项目几个月后回来的同学读这一份就够了。
>
> 本文档与代码同步生成于 `c2e8eba` 之后的 `main` 分支，最后修订日期：2026-05-06。

---

## 1. 项目一句话定位

**给三甲医院病案室用的 AI 质控系统**：
- 输入：病案首页 PDF 或前端表单录入的 100+ 字段
- 处理：100+ 条可配置质控规则（DSL），覆盖编码合法性、字段完整性、跨字段一致性、ICD 编码-名称语义匹配等维度
- 输出：违规清单 + 自然语言解释 + ICD 编码推荐对话

核心特色：**规则引擎可扩展**（DSL + 自定义算子）+ **AI 不是必须**（缺 LLM key 时仅 NL→规则、对话助手等附加功能降级，规则引擎本身完整运行）。

---

## 2. 技术栈

```
┌────────────────────┬─────────────────────────────────────────────────┐
│ 层                 │ 技术选型                                        │
├────────────────────┼─────────────────────────────────────────────────┤
│ 前端               │ Vue 3.4 + TypeScript + Vite 5 + Element Plus 2  │
│                    │ vue-router 4 / Pinia 2（暂未使用）              │
├────────────────────┼─────────────────────────────────────────────────┤
│ 后端               │ Spring Boot 3 + Java 17                         │
│                    │ Spring Data JPA + Hibernate                     │
│                    │ Flyway（DDL 演进）                              │
│                    │ langchain4j 0.36.2（LLM 接入）                  │
├────────────────────┼─────────────────────────────────────────────────┤
│ 数据库             │ PostgreSQL 16 + pgvector（向量列）              │
├────────────────────┼─────────────────────────────────────────────────┤
│ AI 模型            │ DeepSeek V3（chat：NL→规则、对话助手）          │
│                    │ 阿里云 DashScope text-embedding-v3（embedding） │
│                    │ Google Gemini（遗留：PDF 多模态抽取，可选）     │
├────────────────────┼─────────────────────────────────────────────────┤
│ 部署               │ Docker Compose（db + backend + frontend + nginx │
│                    │ 四服务一键起；nginx 同时反代后端 8080 与前端 80）│
└────────────────────┴─────────────────────────────────────────────────┘
```

---

## 3. 顶层目录

```
DeepAudit/
├── backend/             ← Spring Boot 后端（核心代码量在这里）
├── frontend/            ← Vue 单页应用
├── docs/                ← 业务文档、ICD 字典 JSONL、对话记录、QA slides
├── nginx/               ← 反向代理配置
├── postgres/init/       ← 容器首次启动时执行的 init SQL（建库、装 pgvector 插件）
├── scripts/             ← 离线工具
│   ├── sample_pdf/      ← 病案首页测试 PDF 生成器（Python，故意注入质控陷阱）
│   └── smoke-test/      ← DashScope 各模型连通性自检脚本
├── data/                ← 运行期产物（uploads PDF、samples、icd 缓存），均 gitignored
├── code/                ← Phase 0 早期 Phase 报告
├── docker-compose.yml   ← 编排文件
├── .env / .env.example  ← 真实密钥（gitignored）/ 模板
└── *.md                 ← 业务说明、架构说明（本文件即其一）
```

---

## 4. 后端 `backend/`

### 4.1 包结构（按职责分层，对应 Spring 经典三层）

```
com.deepaudit/
├── api/                  ← Web 表现层
│   ├── controller/       ← 7 个 REST 控制器
│   ├── dto/              ← 入参/出参传输对象（record / 不持久化）
│   └── exception/        ← 4 个业务异常 + 1 个全局 @RestControllerAdvice
├── config/               ← Spring 配置（LLM bean 装配、属性绑定）
├── engine/               ← 规则引擎核心（无状态、可单测）
├── persistence/          ← 持久化层
│   ├── entity/           ← 7 个 JPA 实体
│   ├── repository/       ← Spring Data JPA 接口
│   └── type/             ← Hibernate UserType（VectorUserType for pgvector）
└── service/              ← 业务编排层（Service / @Tool / AiService 接口）
```

### 4.2 控制器一览（`api/controller/`）

> 全部统一在 `context-path=/api` 下，下表的路径是去掉前缀后的 mapping。

| 控制器 | 路径前缀 | 职责 |
|---|---|---|
| `MedicalRecordController` | `/medical-records` | 病案 PDF 导入 + 表单保存 + 列表查询 + 查看源 PDF |
| `RuleController` | `/rules` | 规则 CRUD + 试算（dry-run）+ 自然语言→DSL 生成 |
| `CheckResultController` | `/check-results` | 取某病案最新违规清单 + SSE 流式解释 + 状态流转 |
| `OperatorTemplateController` | `/operators` | 自定义算子模板 CRUD + bodyDsl AI 推荐 |
| `IcdDictAdminController` | `/admin/icd-dict` | 字典统计 / reload JSONL / **reembed 批量向量化** |
| `IcdAssistantController` | `/icd-assistant` | **ICD 编码对话助手**（DeepSeek + 工具调用） |
| `TestingAssistantController` | `/testing-assistant` | 测试病案 PDF 生成对话（让 LLM 调 sample_pdf） |

### 4.3 持久化（`persistence/entity/`）

| 实体 | 表名 | 作用 |
|---|---|---|
| `MedicalRecordMain` | `medical_record_main` | 病案首页主表（57+ HQMS 字段、软删除、上次查的状态） |
| `MedicalRecordDiagnosis` | `medical_record_diagnosis` | 诊断子表（1 主诊 + N 其他诊断） |
| `MedicalRecordExtra` | `medical_record_extra` | 主表挂的 JSONB 旁路（不入主表的非业务字段） |
| `QcRule` | `qc_rule` | 质控规则定义（DSL 表达式 + 维度 + 严重度 + 启停 + 软删除） |
| `QcOperatorTemplate` | `qc_operator_template` | 自定义算子模板（代码 + 参数列表 + bodyDsl JSON） |
| `CheckResult` | `check_result` | 一行 = 一条违规命中（含写入瞬间的字段快照） |
| `IcdDict` | `icd_dict` | ICD-9-CM-3 / ICD-10 字典（含 1024 维 `name_embedding` 向量列） |

字段层面的演进见 §6 数据库迁移表。

### 4.4 规则引擎（`engine/`）

引擎是**纯函数 + 内存索引**的设计，从不在质控热路径里直接打 DB：

```
              ┌─────────────────────────────┐
              │  RuleEvaluator              │  纯函数 evalBool
              │  (无状态, 线程安全)         │  递归求值 JSON DSL
              └────────┬────────────────────┘
                       │ 依赖注入
       ┌───────────────┼───────────────┐
       ▼               ▼               ▼
┌───────────────┐ ┌──────────────┐ ┌──────────────┐
│ CustomOpera-  │ │ IcdDictCache │ │ Embedding    │
│ torRegistry   │ │ (内存:        │ │ Service      │
│ (内存: code → │ │  code|cat →   │ │ (在线 embed  │
│  bodyDsl)     │ │  name + vec) │ │  查询文本)   │
└───────────────┘ └──────────────┘ └──────────────┘
       ▲                ▲                  ▲
       │ reload         │ reload           │ 调
       │                │                  │
┌───────────────┐ ┌──────────────────┐ ┌─────────────────┐
│ qc_operator_  │ │ icd_dict 表      │ │ DashScope       │
│ template 表   │ │ + V11 embedding_ │ │ text-embedding- │
│               │ │ text 列          │ │ v3 API          │
└───────────────┘ └──────────────────┘ └─────────────────┘
```

| 文件 | 作用 |
|---|---|
| `RuleEvaluator` | DSL 解释器；`evalBool` 递归求值各 op，`evalValue` 处理 RHS 字段/计算值 |
| `RuleDslValidator` | DSL 语法静态校验（保存前 + LLM 生成后双重防线） |
| `CustomOperatorRegistry` | 自定义算子缓存；启动加载 + DB 变更时重载 |
| `IcdDictCache` | ICD 字典内存索引（名称 + 向量），驱动 `icdCodeExists` / `icdNameMatches` / `icdNameSimilar` 三个 op |
| `FieldAccessor` | DSL 字段路径 → MedicalRecord 反射取值 |

#### 已支持的 DSL 算子

| 算子 | 语义 | 数据来源 |
|---|---|---|
| `notNull` / `isNull` | 字段空判 | 病案字段 |
| `eq` / `ne` | 等值 | 字段 vs 字段或常量 |
| `gt` / `gte` / `lt` / `lte` | 数值/日期比较 | 同上 |
| `dateBefore` / `dateAfter` | 日期序 | 同上 |
| `and` / `or` / `not` | 布尔逻辑 | 嵌套 |
| `dateDiffDays` / `ageYears` | 计算值（仅 RHS） | 字段计算 |
| `custom(code, args)` | 调用自定义算子 | DB → bodyDsl → 递归求值 |
| `icdCodeExists(field, category)` | 编码是否在字典里 | IcdDictCache |
| `icdNameMatches(codeField, nameField, category)` | 名称是否与字典精确匹配 | IcdDictCache（字符串 equals） |
| `icdNameSimilar(codeField, nameField, category, threshold)` | 名称-编码语义相似度 ≥ 阈值 | IcdDictCache + 在线 embed |

### 4.5 业务服务（`service/`）

按职能分组：

#### 病案录入 / 解析

| 服务 | 作用 |
|---|---|
| `MedicalRecordImportService` | PDF 导入主流程：校验 → 落盘 → 文本抽取 → 多模态抽取 → 表单结构化 |
| `MedicalRecordExtractor` | Gemini 多模态：把 PDF 页面图喂模型 → 输出结构化字段 |
| `MedicalRecordSaveService` | 表单提交事务：upsert main + diagnosis + extra 三表 |
| `MedicalRecordMockFiller` | 开发用：让 Gemini 编一份"看着合理的"假病案 |
| `PdfRenderingService` | PDF → PNG（PDFBox），DPI 200 → 150 自动降级避超大页 |
| `FileStorageService` | UUID 命名落盘；提供按 ID 反查 PDF 的 URL |

#### 规则管理

| 服务 | 作用 |
|---|---|
| `RuleService` | 规则 CRUD（带 DSL 校验、软删除、启停） |
| `RuleGeneratorService` | 自然语言 → 规则 DSL 端到端：调 `RuleDslGenerator` + JSON 解析 + 自动建算子 |
| `RuleDslGenerator` | langchain4j 接口：纯 SystemMessage 调用 DeepSeek（绕开模板引擎） |
| `OperatorGeneratorService` | 参数列表 + 描述 → bodyDsl JSON |
| `OperatorBodyDslGenerator` | 同上的 langchain4j 接口 |

#### 质控执行

| 服务 | 作用 |
|---|---|
| `CheckService` | 编排"按记录跑所有启用规则"，写 check_result，返回汇总 |
| `ViolationExplainerService` | 把违规 + 上下文喂 LLM 生成中文解释；支持 SSE 流式 + 缓存 |

#### ICD 字典 + 向量召回（本次新加的核心模块）

| 服务 | 作用 |
|---|---|
| `EmbeddingService` | embedding 模型门面，调 DashScope。提供 `embed(String)` 单条 + `embedAll(List<String>)` 批量；缺 key 时 graceful 返回 null |
| `IcdDictLoader` | 启动时把 `docs/icd_dict/*.jsonl` upsert 进 `icd_dict` 表（幂等） |
| `IcdDictEmbeddingService` | **批量向量化字典**：每批 25 条 → DashScope → 写回 `name_embedding`；失败容忍；`reembedMissing` / `reembedAll` 两模式 |
| `IcdLookupTool` | 给 LLM 用的三个 `@Tool`：`searchByName`（向量 Top-K）、`lookupByCode`（精确反查）、`stats`（库存量） |
| `IcdAssistantService` | langchain4j AiService 接口；DeepSeek + IcdLookupTool 工具调用 |

#### 测试辅助

| 服务 | 作用 |
|---|---|
| `TestingAssistantService` | langchain4j AiService 接口；让 LLM 调 SamplePdfTool 按需生成测试 PDF |
| `SamplePdfTool` | `@Tool` 包装，调用 `scripts/sample_pdf/` 的 Python CLI |

### 4.6 配置（`config/`）

| 类 | 作用 |
|---|---|
| `LlmProperties` | `deepaudit.llm.*` 属性绑定；含内嵌 `Embedding` 子配置 |
| `LlmAutoConfiguration` | 装配所有 LLM bean：`ChatLanguageModel`（DeepSeek）/ `EmbeddingModel`（DashScope）/ `RuleDslGenerator` / `TestingAssistantService` / `IcdAssistantService` 等 |

每个 bean 都用 `@ConditionalOnExpression` 守门，**缺对应 API key 时 bean 不注册**，调用方拿到 `null` 或 503，不会让整个应用崩。

### 4.7 异常处理（`api/exception/`）

| 类 | HTTP 状态 |
|---|---|
| `ValidationException` | 400 — 含错误项数组，前端按字段高亮 |
| `NotFoundException` | 404 |
| `ConflictException` | 409 — 唯一约束冲突 / 启用中规则不可删 |
| `ServiceUnavailableException` | 503 — LLM key 缺失 / 上游不可达 |
| `ApiExceptionHandler` | `@RestControllerAdvice`，把上面 4 种翻译成统一 `ErrorBody` JSON |

---

## 5. 前端 `frontend/`

### 5.1 目录

```
frontend/src/
├── App.vue              ← 根组件（router-view + 全局浮动助手）
├── main.ts              ← 入口（createApp + Pinia + Router + Element Plus 样式）
├── router/index.ts      ← 7 条路由
├── layouts/             ← 1 个布局（侧栏 + 主区）
├── views/               ← 7 个页面级组件
├── components/          ← 3 个可复用组件（含两个对话助手）
├── types/               ← TypeScript 类型（病案领域模型）
└── style.css            ← 全局样式
```

### 5.2 路由 / 视图

| 路径 | 视图文件 | 功能 |
|---|---|---|
| `/` | `Landing.vue` | 落地页 + 后端健康探针徽章 |
| `/import` | `RecordImport.vue` | PDF 上传 → AI 抽取 → 100+ 字段表单提交 |
| `/records` | `RecordList.vue` | 病案列表（分页 + 关键词 + 状态筛选） |
| `/results` | `CheckResult.vue` | 单病案违规清单 + SSE 解释 + 状态流转 |
| `/rules` | `RuleConfig.vue` | 规则 CRUD + DSL 编辑器 + NL→DSL |
| `/operators` | `OperatorLibrary.vue` | 自定义算子模板 CRUD + bodyDsl AI 推荐 |
| `/sandbox` | `RuleSandbox.vue` | DSL 沙箱：选预置规则、编辑表达式、试算 |

### 5.3 组件（`components/`）

| 组件 | 类型 | 用途 |
|---|---|---|
| `IcdAssistantFloating.vue` | **全局浮动** | 右下角 FAB + 可拖拽面板，跨路由保留状态；调 `/api/icd-assistant/chat`；位置/对话历史持久化到 localStorage |
| `TestingAssistantDialog.vue` | 模态弹窗 | RecordImport 页面里使用，让 LLM 调用脚本生成测试 PDF |
| `RuleEditorDrawer.vue` | 抽屉 | 规则 CRUD 表单；NL→DSL 输入；DSL 校验 |

### 5.4 布局（`layouts/AppLayout.vue`）

```
┌──────────┬──────────────────────────────────────┐
│ 左侧栏   │ 顶栏（页面标题）                     │
│ 220px    ├──────────────────────────────────────┤
│ 暗色     │                                      │
│          │                                      │
│ 病案导入 │   <router-view />                    │
│ 病案列表 │                                      │
│ 检查结果 │                                      │
│ 规则配置 │                                      │
│ 算子库   │                                      │
│ 规则沙盒 │                                      │
│          │                                      │
└──────────┴──────────────────────────────────────┘
                                       右下角悬浮 ↘
                                ┌──────┐
                                │ ICD  │ ← IcdAssistantFloating
                                │ 助手 │   挂在 App.vue，跨路由不卸载
                                └──────┘
```

### 5.5 类型 / 数据模型（`types/medicalRecord.ts`）

`MedicalRecord` 接口聚合了 100+ 个 HQMS 字段，分 8 组：身份 / 人口学 / 地址联系方式 / 入出院 / 病房 / 诊断 / 手术 / 费用 & QC。配套 `emptyRecord()` 工厂、`GENDER_OPTIONS` 等枚举、`BUSINESS_FIELDS` 元数据驱动表单生成。

### 5.6 接口约定

- 没有统一的 axios/api 客户端层，所有视图组件**直接 `fetch('/api/...')`**
- Vite dev 启动时 `/api` 代理到 `http://localhost:8080`（见 `vite.config.ts`）
- 容器部署时 nginx 同时反代后端与前端

---

## 6. 数据库

### 6.1 Flyway 迁移演进

| 版本 | 摘要 |
|---|---|
| **V1** | 初始 5 张表 + pgvector 插件守护 + R001/R002 种子规则 |
| **V2** | embedding 列 1024→768（适配 Gemini，已废弃） |
| **V3** | 自定义算子模板表 |
| **V4** | 种子规则 R004：`icdCodeExists` 主手术编码合法性 |
| **V5** | 种子规则 R005：`icdNameMatches` 主手术编码-名称精确一致 |
| **V6** | 主表扩 27 个 HQMS 字段（民族、籍贯、联系方式等） |
| **V7** | 诊断子表（1 主 + N 其他），主表保留 `main_diagnosis_*` 兼容老规则 |
| **V8** | 门诊诊断 4 列扩展 |
| **V9** | 字段重构：删 9 个手术/费用列，补 21 个补充列 |
| **V10** | embedding 列 768→1024（切回 DashScope） |
| **V11** | `icd_dict.embedding_text` 列（含 aliases 的 embed 语料） |
| **V12** | 种子规则 R006：`icdNameSimilar` 主手术编码-名称语义相似 |

### 6.2 关键表关系

```
medical_record_main ─┬─< medical_record_diagnosis (1:N)
                     └─── medical_record_extra (1:1, JSONB)
                                                  │
qc_rule ────────────────< check_result >────────  │
   │                                              │
   │ DSL 引用                                     │
   ▼                                              │
qc_operator_template (自定义算子)                  │
                                                  │
icd_dict (字典 + 1024 维向量) ←── 引擎 + 助手共享 ─┘
```

### 6.3 pgvector 集成

`icd_dict.name_embedding` 与 `qc_rule.description_embedding` 是 `VECTOR(1024)` 列，通过 `VectorUserType`（Hibernate UserType）桥接 Java `float[]` 与 pgvector `[a,b,c]` 字符串协议。

实际读写都走该 UserType，但**质控热路径不打 DB**——`IcdDictCache` 在启动时整表扫到内存 `Map<"code|category", float[]>`，规则求值时直接查 Map。

---

## 7. 配置与部署

### 7.1 Spring 配置文件分层

```
application.yml          ← 基础配置（所有环境共享）
  ├── 数据源、JPA、Flyway、actuator
  ├── deepaudit.icd.* （字典 auto-load）
  ├── deepaudit.llm.* （DeepSeek 占位符）
  └── deepaudit.llm.embedding.* （DashScope 占位符）

application-prod.yml     ← spring.config.import 无条件叠加
  └── 仅 ${VAR:} 占位符，目前只覆盖 chat / embedding api-key

application-docker.yml   ← SPRING_PROFILES_ACTIVE=docker 时叠加
  └── datasource 指向 docker 服务名 `db:5432`
```

**所有真实密钥只放在宿主 `.env`**（gitignored），由 `docker-compose.yml` 的 `environment:` 段透传到容器环境变量，再被 yml 占位符解析。

### 7.2 环境变量

| 变量 | 必需？ | 用途 |
|---|---|---|
| `DEEPSEEK_API_KEY` | 选填 | 缺失 → NL→DSL / 各类对话助手 / 错误解释降级；规则引擎不受影响 |
| `DASHSCOPE_API_KEY` | 选填 | 缺失 → ICD 字典向量化、R006、`searchByName` 降级 |
| `GEMINI_API_KEY` | 选填 | 仅 PDF 多模态抽取等遗留路径 |
| `SPRING_DATASOURCE_*` | docker 环境必需 | 数据库连接，docker-compose 已注入 |
| `DEEPAUDIT_ICD_AUTO_LOAD` | 选填 | 默认 true；置 false 禁用启动时灌字典 |

### 7.3 Docker Compose 编排

```
nginx :8080  ← 对外唯一暴露端口
   │
   ├── /api/* → backend :8080
   └── /*     → frontend :80（Vue 构建产物 nginx 静态托管）
                                │
backend ──► db :5432 (pgvector/pgvector:pg16)
   │                                    ▲
   │                                    │ Flyway 启动时跑 V1..V12
   ├── DASHSCOPE → text-embedding-v3
   ├── DEEPSEEK  → deepseek-chat
   └── 挂载 backend_uploads 卷保存 PDF
```

启动：`docker compose up -d --build`，约 1 分钟全栈就绪。

---

## 8. 核心数据流（端到端）

### 8.1 病案导入 → 质控 → 解释

```
医生 ─[上传 PDF]→ RecordImport.vue
                           │ POST /api/medical-records/import
                           ▼
              MedicalRecordImportService
                           │
                           ├── FileStorageService    （落盘 UUID 命名）
                           ├── PdfRenderingService   （PDF → PNG 页）
                           └── MedicalRecordExtractor（Gemini 多模态）
                                       │
                                       ▼
                                  抽取的字段 JSON ─► 前端表单回填
                                                      │ 用户审核 / 修改
                                                      │ POST /api/medical-records
                                                      ▼
                                       MedicalRecordSaveService
                                       （main + diagnosis + extra 三表事务）
                                                      │
                                                      ▼
                                                  CheckService
                                       （加载所有启用规则 + RuleEvaluator 求值）
                                                      │
                                                      ▼
                                                check_result 表
                                                      │
                                                      ▼
                                                CheckResult.vue
                                                （列表展示 + 点击解释）
                                                      │ GET /api/check-results/:id/explanation (SSE)
                                                      ▼
                                          ViolationExplainerService
                                           （DeepSeek 流式解释）
```

### 8.2 ICD 字典向量化（一次性离线）

```
docs/icd_dict/*.jsonl
       │ Maven build 镜像到 classpath
       ▼
IcdDictLoader   @PostConstruct
       │ upsert
       ▼
icd_dict 表（name_embedding=NULL）
       │
       │ 运维触发：POST /api/admin/icd-dict/reembed
       ▼
IcdDictEmbeddingService
   ├── 25 条/批 → EmbeddingService.embedAll
   │             → DashScope text-embedding-v3 (1024 维)
   ├── 写回 name_embedding
   └── 完成后 IcdDictCache.reload()
       │
       ▼
   内存 Map<"code|cat", float[]> 就绪
       │
       ├──► RuleEvaluator.icdNameSimilar 在线读
       └──► IcdLookupTool.searchByName    在线读
```

### 8.3 ICD 编码对话助手（在线）

```
医生 ─[在任意页面右下角点 FAB]→ IcdAssistantFloating.vue
                                       │ POST /api/icd-assistant/chat
                                       │  { "message": "腹腔镜阑尾切除是什么编码？" }
                                       ▼
                       IcdAssistantController → IcdAssistantService (langchain4j AiService)
                                                       │
                              系统提示 + 用户消息 + IcdLookupTool 工具描述 → DeepSeek
                                                       │
                              DeepSeek 决定调 searchByName("腹腔镜阑尾切除", "icd9cm3", 5)
                                                       │
                                                       ▼
                       IcdLookupTool.searchByName
                          ├── EmbeddingService.embed(query) → DashScope
                          └── IcdDictCache.topKByCosine(...) → 内存 cosine 排序
                                                       │
                              返回 "47.0100|腹腔镜下阑尾切除术|0.92, ..." 给 DeepSeek
                                                       │
                              DeepSeek 综合 → 中文回复 ─► reply 字段
                                                       │
                                                       ▼
                                  对话面板渲染（保留滚动位置 + 历史）
```

---

## 9. 模块依赖关系（高层视图）

```
                 ┌────────────────┐
                 │   Controllers  │  REST 入口，仅做参数校验 + 转发
                 └───────┬────────┘
                         │
            ┌────────────┴────────────┐
            ▼                         ▼
   ┌──────────────────┐   ┌──────────────────────┐
   │   Services       │   │   AiService 接口     │ langchain4j 工具调用
   │   (业务编排)     │   │  (RuleDsl, Testing,  │ 由 LlmAutoConfig 装配
   │                  │   │   IcdAssistant)      │
   └────────┬─────────┘   └──────────┬───────────┘
            │                        │
            ├────────────┬───────────┴──────────┐
            ▼            ▼                      ▼
   ┌─────────────┐ ┌────────────┐       ┌──────────────┐
   │   engine/   │ │ Repository │       │  @Tool 类    │
   │ (规则引擎)  │ │   (JPA)    │       │  (SamplePdf, │
   │             │ │            │       │   IcdLookup) │
   └──────┬──────┘ └──────┬─────┘       └──────┬───────┘
          │               │                    │
          │               ▼                    │
          │      ┌──────────────────┐          │
          │      │  PostgreSQL +    │          │
          │      │  pgvector        │          │
          │      └──────────────────┘          │
          │                                    │
          └────► IcdDictCache / Custom ◄───────┘
                 OperatorRegistry / Embedding
                 (启动时构建, 内存索引)
```

---

## 10. 扩展点

下次给项目加东西时知道改哪里：

| 我想加…… | 改这些地方 |
|---|---|
| **一条新质控规则**（纯 DSL 能表达） | 写一条 `Vxx__seed_rule_Rxxx.sql` migration |
| **一个新 DSL 算子**（Java-backed） | ① `RuleEvaluator.evalBool` 加 `case`；② `RuleDslValidator` 白名单 + 校验；③ 单测 |
| **一个新自定义算子**（DSL 组合） | 前端 `OperatorLibrary.vue` 创建即可，不改 Java |
| **一个新对话助手** | ① `XxxLookupTool` 写 `@Tool`；② `XxxAssistantService` interface；③ `prompts/xxx.system.txt`；④ `LlmAutoConfiguration` 注册 bean；⑤ 控制器；⑥（可选）前端浮动入口 |
| **一个新 ICD 字典源** | 在 `docs/icd_dict/` 加 JSONL；`IcdDictLoader.JSONL_FILES` 加文件名；reload + reembed |
| **切换 embedding 模型** | 改 `application.yml: model: <new>`；如果维度变了写 `Vxx__resize.sql` 调列宽 + `POST /reembed?mode=all` 全量重算 |
| **病案首页加字段** | ① Flyway 加列；② `MedicalRecordMain` 实体加字段；③ `MedicalRecord` TS 类型加字段；④ 前端表单加输入框；⑤（如果要参与质控）`FieldAccessor` 自动 work |

---

## 11. 已知尾巴 / 下一步

按重要度排序，未做完的事项：

1. **历史中的 Gemini key `AIzaSyB...JI8A` 仍在 git history**，需要在 Google AI Studio 作废，否则虽然代码已清理，攻击者从 history 仍可拿到。
2. **MVP 字典精选 ~227 条**，正式部署前需用国卫办医函〔2020〕438 号全量 8000+ 条手术 + 30000+ 条诊断替换 JSONL。预计向量化耗时 5-10 分钟、费用 ~￥3-5。
3. **R-Std-002 ICD 版本一致性**规则尚未落地（loader_spec 提过示例，未入库）。
4. **前端无 axios 封装**，所有 fetch 散落在视图里——错误处理、加载态、token 注入未来如要统一较麻烦。
5. **Pinia 装了未用**——目前所有页面状态都在组件本地，未跨页面共享。如对话助手历史想跨刷新保留可考虑。

---

## 附录 A：常用运维命令

```bash
# 起全栈
docker compose up -d --build

# 看后端启动日志（确认 LLM bean 是否成功装配）
docker compose logs -f backend | head -50

# 字典统计 + 向量覆盖率
curl http://localhost:8080/api/admin/icd-dict/stats

# 字典向量化（首次或 JSONL 改动后）
curl -X POST http://localhost:8080/api/admin/icd-dict/reembed

# 直接验证向量入库
docker compose exec db psql -U deepaudit -d deepaudit \
  -c "SELECT code, name, vector_dims(name_embedding) FROM icd_dict WHERE name_embedding IS NOT NULL LIMIT 3;"

# 试用 ICD 助手
curl -X POST http://localhost:8080/api/icd-assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"腹腔镜阑尾切除是什么编码？"}'

# 给一条病案跑全部规则（CheckService 入口）
curl -X POST http://localhost:8080/api/medical-records/{id}/check
```

## 附录 B：关键文件速查

| 我想看 | 文件路径 |
|---|---|
| 规则 DSL 怎么求值 | `backend/.../engine/RuleEvaluator.java` |
| ICD 字典怎么进库 | `backend/.../service/IcdDictLoader.java` |
| 字典怎么向量化 | `backend/.../service/IcdDictEmbeddingService.java` |
| LLM bean 怎么装配 | `backend/.../config/LlmAutoConfiguration.java` |
| ICD 助手系统提示 | `backend/src/main/resources/prompts/icd_assistant.system.txt` |
| 字典 JSONL 数据 | `docs/icd_dict/*.jsonl` |
| 浮动对话框组件 | `frontend/src/components/IcdAssistantFloating.vue` |
| 规则编辑器 | `frontend/src/components/RuleEditorDrawer.vue` |
| 病案领域模型 | `frontend/src/types/medicalRecord.ts` |

---

> 修改本文档时请记得：每次大改架构（新增表 / 切换 LLM / 新增模块）后回来同步一遍，
> 否则 6 个月后这份文档就开始撒谎了。
